function rectangleAreaTotal (base, height){
    const rectangleArea = base * height;
    return rectangleArea;
}
let base = 10;
let height = 5;
const rectangleAreaIs = rectangleAreaTotal(base, height)
console.log('Total area:', rectangleAreaIs)